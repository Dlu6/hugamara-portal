// // src/features/extension/extensionSelectors.js
// import { createSelector } from "@reduxjs/toolkit";

// export const selectExtension = (state) => state.extension;

// export const selectExtensionStatus = createSelector(
//   selectExtension,
//   (extension) => extension.status
// );

// export const selectCallState = createSelector(
//   selectExtension,
//   (extension) => extension.call
// );

// export const selectContactStatus = createSelector(
//   selectExtension,
//   (extension) => extension.contact
// );

// export const selectConnectionStatus = createSelector(
//   selectExtension,
//   (extension) => extension.status.connection
// );

// export const selectPresenceState = createSelector(
//   selectExtension,
//   (extension) => extension.status.presence
// );

// export const selectRegistrationStatus = createSelector(
//   selectExtension,
//   (extension) => extension.status.registration
// );

// export const selectLastUpdate = createSelector(
//   selectExtension,
//   (extension) => extension.timestamp
// );
